# tron-8004-sdk

Monorepo-style layout for multi-language SDKs.

## Structure

- `python/`: Python SDK (current implementation)
- `ts/`: Reserved for future TypeScript SDK

## Python SDK

```bash
cd python
pip install -e .
```

See `python/README.md` for usage and samples.
